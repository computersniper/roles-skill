# Dependencies for tools (optional, only needed for the Python helper scripts)
# Most functionality works without these if you're using Claude Code native tools

# For the Python tools (required when running tool generation and existing tools):
requests>=2.25.0
